import { CSSProperties } from 'react';
export declare type ViewStyles = {
    drawerStyle?: CSSProperties;
} & CSSProperties;
declare const light: ViewStyles;
declare const dark: ViewStyles;
export { light, dark };
